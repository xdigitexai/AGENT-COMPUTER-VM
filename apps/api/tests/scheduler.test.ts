import { describe,expect,it,vi } from "vitest";
import { scheduleHost } from "../src/services/scheduler.js";
describe("scheduler",()=>{it("filters maintenance and insufficient hosts then selects lowest allocation",async()=>{const findMany=vi.fn().mockResolvedValue([{id:"full",enabled:true,credential:{},health:"HEALTHY",maintenanceMode:false,cpuCapacity:4,allocatedCpu:4,ramCapacityMb:4096,allocatedRamMb:0,storageCapacityGb:100,allocatedStorageGb:0},{id:"maintenance",enabled:true,credential:{},health:"HEALTHY",maintenanceMode:true,cpuCapacity:16,allocatedCpu:0,ramCapacityMb:32000,allocatedRamMb:0,storageCapacityGb:1000,allocatedStorageGb:0},{id:"good",enabled:true,credential:{},health:"HEALTHY",maintenanceMode:false,cpuCapacity:16,allocatedCpu:2,ramCapacityMb:32000,allocatedRamMb:4000,storageCapacityGb:1000,allocatedStorageGb:20}]);const host=await scheduleHost({computeHost:{findMany}} as any,{provider:"proxmox",vcpu:2,ramMb:2048,storageGb:20});expect(host.id).toBe("good")});it("fails explicitly when capacity is unavailable",async()=>{await expect(scheduleHost({computeHost:{findMany:vi.fn().mockResolvedValue([])}} as any,{provider:"proxmox",vcpu:2,ramMb:2048,storageGb:20})).rejects.toMatchObject({code:"INSUFFICIENT_CAPACITY"})})});
